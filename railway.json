{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn --bind 0.0.0.0:$PORT main:app --timeout 120 --workers 1",
    "restartPolicyType": "ON_FAILURE"
  }
}
